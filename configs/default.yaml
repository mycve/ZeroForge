# ZeroForge 配置

seed: 42

# 网络
network:
  channels: 128
  num_blocks: 10

# MCTS
mcts:
  num_simulations: 32

# 训练
training:
  batch_size: 256
  learning_rate: 0.0002
  selfplay_steps: 100

# 检查点
checkpoint:
  dir: "checkpoints"
  keep: 5
