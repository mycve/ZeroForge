# ZeroForge 配置
# 大部分参数已硬编码在 train.py 中

seed: 42

# 网络
network:
  channels: 128
  num_blocks: 10

# MCTS
mcts:
  num_simulations: 64

# 训练
training:
  batch_size: 1024
  learning_rate: 0.0002
  selfplay_steps: 200

# 检查点
checkpoint:
  dir: "checkpoints"
  keep: 5
